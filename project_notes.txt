Completed steps:
- python script to launch a transient Debian VM based on a YAML file
- helper python script to clean up the transient VM files after shutdown
- developed a shell script to launch the backing VM
- configured networking on backing image and child images
- updated packages of backing image from install .iso
- edit helper python script to properly clean up,

Next steps:
- configure working directory for new shells
- fix perm VM tool; launching throws error:
	- ERROR    Disk /data/VMs/debian/debian-vm-test.qcow2 is already in use by other guests ['debian-test-vm']. (Use --check path_in_use=off or --check all=off to override) 

Notes:
- launch backing VM and child VMs with sudo
- backing mac is persistent at: 52:54:00:e3:31:66
-  

Commands:
- sudo bin/start_<os>_backing_vm.sh
- sudo bin/yaml_launched_vm_temp.py yaml/debian_test_vm_temp.yml
- virsh attach-interface --domain centos9_backing_temp --source default --type network --config
- sudo virsh undefine debian-test-vm
- sudo bin/yaml_launched_vm_perm.py yaml/debian_test_vm_perm.yml
### connect to perm vm:
- virsh --connect qemu:///system start debian-test-vm
- sudo virt-viewer debian-test-vm
- virsh managedsave-remove fedora_coreos
- virsh undefine fedora-coreos-stable
- virsh undefine debian-test-vm (use this when done with a perm VM)
### troubleshooting
- virsh console debian-test-vm
- virt-viewer debian-test-vm
- virsh shutdown debian-test-vm
- virsh destroy debian-test-vm

